target C {
    single-threaded: true
}

preamble {=
    #include <servo.h>
=}

reactor Servo {
    input angle: int

    reaction(startup) {=
        servo_init();
    =}

    method set_angle(angle) {=
        servo_set_angle(angle);
    =}
}